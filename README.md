# Cloudflare + GAS Starter

1. Deploy Cloudflare Pages (connect repo)
2. Replace GAS URL in functions/api/[[path]].js
3. Deploy GAS as Web App (Anyone)

Routes:
- / -> index
- /view?id=123 -> view data
- /api/* -> proxy to GAS
